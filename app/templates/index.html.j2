"""
Jinja2 HTML template for optional server-rendered preview
(used if you extend the FastAPI UI beyond Streamlit).
"""

TEMPLATE = """
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>{{ app_name }}</title>
  <link rel="stylesheet" href="/static/css/styles.css" />
</head>
<body>
  <header class="tp-hero">
    <h1>{{ app_name }}</h1>
    <p>Offline tender PDF extraction</p>
  </header>
  <main>
    {% if result %}
      <section class="tp-card">
        <h2>{{ result.tender_information.tender_name or "Tender" }}</h2>
        <pre>{{ result_json }}</pre>
      </section>
    {% else %}
      <p>No result yet.</p>
    {% endif %}
  </main>
</body>
</html>
"""
